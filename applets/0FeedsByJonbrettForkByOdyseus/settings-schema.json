{
    "heading1": {
        "type": "header",
        "description": "Feed URLs"
    },
    "manage_feeds_button": {
        "type": "button",
        "description": "Manage Feeds...",
        "callback": "manage_feeds",
        "tooltip": "Launch the feeds manager application to add/remove/edit feeds."
    },
    "pref_overlay_key": {
        "type": "keybinding",
        "default": "",
        "description": "Keyboard shortcut to open and close the menu."
    },
    "pref_new_feed_icon": {
        "type": "iconfilechooser",
        "default": "feeds-reader-rss-new-feed-symbolic",
        "description": "Icon used when there are new feeds",
        "tooltip": "Select an icon to show in the panel."
    },
    "pref_feed_icon": {
        "type": "iconfilechooser",
        "default": "feeds-reader-rss-feed-symbolic",
        "description": "Icon used when there are not new feeds",
        "tooltip": "Select an icon to show in the panel."
    },
    "pref_max_items": {
        "type": "spinbutton",
        "default": 10,
        "min": 1,
        "max": 20,
        "step": 1,
        "units": "feeds",
        "description": "Maximum number of items",
        "tooltip": "Maximum number of items to display for each feed."
    },
    "pref_refresh_interval_mins": {
        "type": "spinbutton",
        "default": 30,
        "min": 1,
        "max": 1440,
        "step": 1,
        "units": "minutes",
        "description": "Update interval",
        "tooltip": "Interval between checking for new items."
    },
    "pref_description_max_length": {
        "type": "spinbutton",
        "default": 2000,
        "min": 1000,
        "max": 10000,
        "step": 500,
        "units": "characters",
        "description": "Description maximum length"
    },
    "pref_tooltip_max_width": {
        "type": "spinbutton",
        "default": 500,
        "min": 100,
        "max": 2000,
        "step": 10,
        "units": "pixels",
        "description": "Tooltip maximum width"
    },
    "pref_notifications_enabled": {
        "type": "checkbox",
        "default": true,
        "description": "Show notifications for new items",
        "tooltip": "If enabled, a popup notification will be displayed for new items."
    },
    "pref_unified_notifications": {
        "type": "checkbox",
        "default": true,
        "dependency": "pref_notifications_enabled",
        "description": "Unified notifications",
        "tooltip": "If enabled, all feeds notifications will use and update one notification."
    },
    "pref_enable_verbose_logging": {
        "type": "checkbox",
        "default": false,
        "description": "Verbose logging",
        "tooltip": "Checking this will enable debug level logging."
    },
    "pref_profile_name": {
        "type": "generic",
        "default": "Default",
        "description": "Feed Profile Name."
    },
    "pref_last_checked_storage": {
        "type": "generic",
        "default": {},
        "description": "Storage for last feeds checked values."
    }
}
